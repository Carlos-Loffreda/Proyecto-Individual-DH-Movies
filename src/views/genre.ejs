<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h2><%= genre.name %></h2>
    
    <h4> Películas del género <%= genre.name%>:</h4>
    <% movieArray.forEach(movie => { %>
        
        <ul>
        <li><p><%= movie.title %></p></li>
        </ul>
    <% }) %>
              
</body>
</html>